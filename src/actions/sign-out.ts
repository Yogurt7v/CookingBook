import { signOut } from '@/auth/auth';

export default async function signOutFunc() {
  // try {
  //   const result = await signOut({
  //     redirect: false,
  //   });
  //   return result;
  // } catch (error) {
  //   console.error(error);
  //   throw error;
  // }
}
